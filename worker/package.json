{
  "name": "cm-data-viewer-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --assets ./site",
    "deploy": "wrangler deploy --assets ./site"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}
